# NeuronX API key — sent as X-API-Key header on every request
VITE_NEURONX_API_KEY=your_api_key_here

# Optional: override API base URL if not using the nginx/Vite proxy
# Leave unset to use the built-in proxy (recommended)
# VITE_NEURONX_API_URL=https://neuronx.jagatab.uk
